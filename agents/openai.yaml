interface:
  display_name: "Health Planner"
  short_description: "轻量记录个人健康状态，并按每日、7日和14日提供循证反馈"
  default_prompt: "使用 $health-planner 导入我以前的健康记录，确认基线，并继续记录今天的健康情况。"
policy:
  allow_implicit_invocation: true
